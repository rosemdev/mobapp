{
  "name": "mobapp",
  "version": "1.0.0",
  "description": "first landing page",
  "main": "index.js",
  "scripts": {
    "parcel": "node_modules/.bin/parcel ./src/index.html --public-url ./",
    "build": "node_modules/.bin/parcel build ./src/index.html --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "rosemdev"
  },
  "author": "rosem",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "less": "^3.0.1"
  }
}
